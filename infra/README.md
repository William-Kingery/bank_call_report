# Bank Call Report CDK Infra

Infrastructure code for this project lives in this directory (`infra/`).

The deployment plan and runbook were moved to:

- [`docs/CDK_DEPLOYMENT_PLAN.md`](../docs/CDK_DEPLOYMENT_PLAN.md)

That document contains prerequisites, first deploy steps, API URL wiring, validation, and operational commands.
